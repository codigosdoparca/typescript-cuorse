import { testDouble, expect } from './config/helpers';
import User from '../../server/modules/User/service';

describe('Testes Unitários do Controller', () => {

   /* 

   describe('Método Create', () => {
        it('Deve criar um novo usuário', () => {

            const novoUsuaerio = {
                id: 1,
                name: 'Novo Usuário',
                email: 'novousuario@email.com',
                password: '1234'
            }

            const user = new User();
            return user.create(novoUsuaerio)
                        .then(data => {
                            expect(data.dataValues).to.have.all.keys(
                                ['email', 'id', 'name', 'password', 'updatedAt', 'createdAt']
                            )
            });
        });
    });

    describe('Método GET Users', () => {
        it('Deve retornar uma lista com todos os usuários usuário', () =>{

            const user = new User();

            return user.getAll().then(data =>{
                expect(data).to.be.an('array');
                expect(data[0]).to.have.all.keys(
                    ['email', 'id', 'name', 'password']
                )
            })
        });
    });

    
    describe('Método GET Users', () => {
        it('Deve atualizar um usuário', () =>{

        });
    });

    describe('Método update', () => {
        it('Deve atualizar um usuário', () => {

            const usuarioAtualizado = {
                name: 'Nome Atualizado2',
                email: 'email@testeatualizado.com'
            };

            const user = new User();
            return user.update(1, usuarioAtualizado).then(data => {
                expect(data[0]).to.be.equal(1);
            })
        });
    });

      describe('Método Delete', () => {
        it('Deve deletar um usuário', () =>{
            const user = new User();
            return user.delete(1).then(data => {
              //  expect(data[0]).to.be.gt(0);
              expect(data[0]).to.be.equal(1);

            })
        });
    });



    describe('Método getById', () => {
        it('Deve buscar um usuario de acordo com o id', () => {
            const user = new User();

            return user.getById(1).then(data => {
                expect(data[0]).to.have.all.keys(
                    ['email', 'id', 'name', 'password']
                )
            })
        });
    });
    

    describe('Método getByEmail', () => {
        it('Deve buscar um usuario de acordo com o email', () => {
            const user = new User();

            return user.getByEmail('"novousuario@email.com"').then(data => {
                expect(data[0]).to.have.all.keys(
                    ['email', 'id', 'name', 'password']
                )
            })
        });
    });
    
*/
    
});

